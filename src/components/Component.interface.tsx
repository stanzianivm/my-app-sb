export interface MyComponentProps {
  /** An additional style class name to assign on component. */
  additionalClassName?: string;
  /** The ID used to test the component on unit tests. */
  testId?: string;
}
